<h1>Listing rates</h1>

<table>
  <thead>
    <tr>
      <th>Cleanliness</th>
      <th>Quality</th>
      <th>Price</th>
      <th>Punctulity</th>
      <th>Dealing</th>
      <th>Average</th>
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @rates.each do |rate| %>
      <tr>
        <td><%= rate.cleanliness %></td>
        <td><%= rate.quality %></td>
        <td><%= rate.price %></td>
        <td><%= rate.punctulity %></td>
        <td><%= rate.dealing %></td>
        <td><%= rate.average %></td>
        <td><%= rate.comment %></td>
        <td><%= link_to 'Show', rate %></td>
        <td><%= link_to 'Edit', edit_rate_path(rate) %></td>
        <td><%= link_to 'Destroy', rate, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'New Rate', new_rate_path %>
